<%= simple_form_for(@path) do |f| %>
  <%= f.input :name, label: 'Projekt' %>
  <%= f.input :description, label: 'Beschreibung' %>
  <%= f.input :hours, label: 'Stunden' %>
  <%= f.button :submit %>
<% end %>
